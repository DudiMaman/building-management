# Status 06-2026 — סיכום מצב הפרויקט

> מסמך סטטוס מקיף לפרויקט **Building Management — "Pulse"**.
> מעודכן ל-15 ביוני 2026. נשמר לצד [`SPEC.md`](./SPEC.md) (האפיון) ו-[`PROGRESS.md`](./PROGRESS.md) (מעקב שוטף).

---

## 1. מבט-על

פלטפורמת SaaS CRM עברית-first לחברות ניהול ואחזקת מבנים בישראל. מחליפה ומרחיבה את Darimpo / Bllink / Build-app. מונורפו (Turborepo + pnpm) עם 6 אפליקציות ו-6 חבילות משותפות.

**מצב כללי:** ה-Backend, ה-Admin Web ושתי אפליקציות המובייל **מומשו וחוברו מקצה-לקצה ומאומתים** (typecheck/בדיקות/build ירוקים). נותרו פריטים שתלויים בחשבונות/credentials חיצוניים ובאימות חזותי בהרצה.

**אימות נוכחי:** `typecheck 16/16 · 57 בדיקות עוברות (40 api + 10 shared + 7 ai) · build 8/8 · typecheck נקי לשתי אפליקציות המובייל.`

---

## 2. ארכיטקטורה ומחסנית

| שכבה | טכנולוגיה |
|---|---|
| מובייל | React Native + Expo SDK 51 (דייר + אחזקה) |
| Backend | Node 20 + NestJS 10 + TypeScript 5 (מונוליט מודולרי, 25+ מודולים) |
| DB | Postgres 15 (Supabase, eu-west-1), RLS על כל הטבלאות, 15 מיגרציות |
| תשלומים | **Tranzila** (טכנולוגיית הסליקה) + **MAX** (סולק) — ראו ADR-001 |
| AI | Anthropic Claude (`claude-sonnet-4-6`) |
| WhatsApp | Meta WhatsApp Business Cloud API |
| Web | Next.js 14 (marketing + admin), Tailwind, RTL מלא |
| Desktop | Electron (עטיפת ה-Admin) |
| תורים | BullMQ + Redis (worker נפרד + cron) |

**Multi-tenancy:** DB משותף + `tenant_id` בכל שורה + Postgres RLS. הקשר ה-tenant מוזרק לחיבור דרך JWT claims.

---

## 3. החלטה ארכיטקטונית מרכזית — ADR-001 (חיוב)

הפלטפורמה היא שכבת ניהול/CRM/תקשורת. **ביצוע החיוב מואצל ל-Tranzila** (הוראות קבע, חיובים מחזוריים, retries, ניהול חייבים, סליקה דרך MAX). **לא בנינו מנוע גבייה משלנו.** מה שבבעלותנו: מודל החשבונית/חיוב, **חשבונית-מס ישראלית תואמת-רשות-המסים**, אינטגרציה דו-כיוונית מלאה מול Tranzila (שליחה + webhooks חתומים ואידמפוטנטיים + סנכרון סטטוסים), ודוחות. פירוט מלא: [`docs/decisions.md`](./docs/decisions.md).

---

## 4. מצב לפי מודולים

### ✅ Backend — מומש ועובד
- **דומיין + סכמה + RLS (§3–4):** כל הישויות, אלגוריתם bill-payer (§3.6.4, נבדק 4 תרחישים), טריגרים, אינדקסים.
- **Auth (§5):** JWT guard, RolesGuard, **hook ל-claims** (מיגרציה 0015 — מזריק tenant_id/role/person_id), flow הזמנות mgmt/worker, QR claim לדיירים.
- **חיוב + תשלומים (§11):** מחזורי חיוב, dry-run, Tranzila adapter (live+mock), iframe, **capture אידמפוטנטי → סימון חיוב משולם → קבלת-מס אוטומטית**.
- **חשבוניות (§37):** מספור רציף gap-free אטומי, חשבוניות זיכוי, ITA clearance (live+mock), PDF.
- **צ'קים (§38):** קליטה, הפקדות, **צ'ק חוזר → היפוך חיוב + עמלה + התראה קריטית**.
- **ספקים + MASAV (§12):** ספרייה + יצירת קובץ.
- **קריאות שירות (§18):** סיווג AI, חלון SLA, סטטוסים, דירוג שביעות-רצון בסגירה.
- **משימות (§17):** יצירה אוטומטית מקריאה + שיוך לפי skills/עומס.
- **מסמכים (§39):** העלאה+גרסאות, OCR (Claude vision), **חיפוש FTS**, **cron תפוגה → משימת חידוש**, ACL מדורג לדיירים.
- **WhatsApp + בוט (§13–14):** קבלה→בוט→מענה end-to-end, לולאת tool-use רב-שלבית, 7 כלים, HMAC, **כוונון per-tenant (טון/חוקים)**.
- **התראות (§21):** fanout לפי notification_policy + העדפות, ערוצי push/email/SMS/WhatsApp, dedupe, DND, rate-limit.
- **לוח מודעות (§15):** הפצה בפרסום + פיד מסונן-audience לדיירים.
- **סקרים (§16):** זכאות לפי תפקיד, אנונימיות HMAC, חתימות דיגיטליות, **רישום הצבעות ל-audit chain**.
- **תוספים (§19):** קטלוג + checkout דרך צינור החיוב + lifecycle.
- **שער גישה (§20):** פתיחה one-tap, קודי אורח, audit.
- **דוחות (§22):** collection rate, AR aging, per-person AR, cash-flow, פריון עובדים, הכנסות תוספים, **ייצוא CSV**.
- **ביקורת (§24):** **interceptor גלובלי → hash-chain**, endpoints ציות (export/erase).
- **תשתית (§26–27):** worker נפרד + cron (Asia/Jerusalem), Pino logging, exception filter גלובלי + Sentry אופציונלי, rate-limiting.

### ✅ Admin Web — מחובר במלואו
כל העמודים מחוברים ל-API (אפס placeholders): Dashboard, Buildings, Apartments, People, Rental-contracts, Tasks, Tickets, Charges, Invoices, **Checks**, **Documents** (חיפוש OCR), Vendors, Bulletin, **Polls**, **WhatsApp inbox**, **Notifications**, **AI-bot tuning**, Reports, Audit-log, Settings. Auth דרך `@supabase/ssr`.

### ✅ מובייל
- **דייר:** Home (יתרה/בניין/קריאות חיות), Charges (+ קישור תשלום), Tickets, Bulletin, שליחת פנייה — מחוברים דרך `/me/*`.
- **אחזקה:** מסך "היום" מחובר ל-`/workers/me/today` + פעולות start/done.
- **Desktop:** עטיפת Electron של ה-Admin.

### ✅ אתר שיווק
אתר Pulse מלא (Next.js, RTL), כולל לוגו מעוצב עם אנימציית "פעימה", **deployed ל-GitHub Pages** ומתעדכן אוטומטית מ-main.

---

## 5. מה שנותר

### דורש החלטה/חשבון שלך (👤)
- **Credentials לפרודקשן:** Supabase · Tranzila + MAX · Meta WhatsApp · Anthropic · Resend (email) · Inforu/019 (SMS) · רשות המסים (ITA) · Sentry.
- **הפעלת ה-Auth hook** ב-Supabase: Authentication → Hooks → Custom Access Token.

### נדחה במכוון (החלטת המשתמש)
- מפת admin (§23) — דולג.
- RAG וקטורי / embeddings (§14.4) — דולג; חיפוש FTS נשאר פעיל.
- ריבוי שפות (§28) — עברית בלבד.

### לא-חוסם, לעשייה עתידית
- **הרצת אפליקציות מובייל ב-Expo** לאימות חזותי (כרגע typecheck נקי בלבד).
- **חיווט העלאת תמונות** בפנייה (ticket-new) ל-Files service.
- **פונט Heebo TTF** ב-`apps/api/assets/fonts/` כדי ש-PDF עברי ייוצר נכון (כרגע נופל ל-Helvetica).
- העמקת אינטגרציית Tranzila מול sandbox אמיתי + אימות MASAV מול בנק.

---

## 6. הפעלה מקומית

```bash
pnpm install
cp .env.example .env          # מלאו credentials או השאירו ריק ל-mock adapters
pnpm db:migrate && pnpm db:seed
pnpm dev                      # מריץ את כל האפליקציות
pnpm --filter @bm/api start:worker   # תהליך ה-worker (תורים + cron)
```
כל האינטגרציות החיצוניות כוללות **mock adapters** שמתפעלים כשחסרים env vars — המערכת רצה end-to-end בלי credentials.

**בדיקות:** `pnpm turbo run typecheck test build`

---

## 7. מצב Git / Deploy

- ענף עבודה: `claude/great-rubin-CqWBX`. **מוזג ל-`main`** (fast-forward נקי).
- כל push ל-`main` מפעיל את `deploy-marketing.yml` → פרסום ל-`gh-pages`.
- אתר חי: `https://dudimaman.github.io/building-management/`.
- היעד הסופי: extraction לריפו עצמאי `Building-management` (ראו PROGRESS §"Repository extraction").

---

## 8. ציוני דרך (Milestones)
M0 תשתית · M1 Auth+דומיין · M2 חיוב+חשבוניות+צ'קים · M3 קריאות+משימות+מסמכים · M4 מובייל+שער · M5 WhatsApp+AI · M6 שיווק+admin — **כולם מומשו**. סבב ההשלמה (יוני 2026) הפך את ה-skeletons לפועלים, חיבר את כל ה-front-ends, והוסיף את הפערים הרוחביים (ביקורת, התראות, ציות, תצפית).
